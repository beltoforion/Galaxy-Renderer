#include "Star.hpp"

#include "MathHelper.hpp"
#include <cmath>


Star::Star()
	: theta(0)
	, velTheta(0)
	, angle(0)
	, a(0)
	, b(0)
	, temp(0)
	, mag(0)
	, center(0, 0)
	, vel(0, 0)
	, pos(0, 0)
{}
